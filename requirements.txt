# 직접 의존성만 명시한다.
# pip freeze 결과 전체를 커밋하면 전이 의존성과 플랫폼 종속 패키지까지 고정되어
# 다른 OS에서 설치가 깨지거나 보안 패치 적용이 막힌다.
#
# 설치:   pip install -r requirements.txt
# 재현용 잠금 파일이 필요한 경우: pip freeze > requirements.lock.txt

streamlit>=1.40
openai>=3.0,<4.0
